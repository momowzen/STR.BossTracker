{
  "name": "bosstracker-discord-auth",
  "version": "1.0.0",
  "description": "Discord OAuth2 callback for Boss Tracker authentication",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": { "node": "18" },
  "main": "index.js",
  "dependencies": {
    "firebase-functions": "^6.0.0",
    "axios": "^1.7.0"
  }
}
